
name: Ezra Deployment Email

on:
  push:
    branches:
      - main

jobs:
  send-email:
    runs-on: ubuntu-latest

    steps:
              - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

